import React  from 'react'
import Nav from './component/nav'
const App =() =>{
 
   
        // const [count, setCount] = useState(0);
       
        return (
            <div>
                <Nav/>
                {/* <p>count : {count}</p>
                <button onClick={()=>{setCount(count+1); console.log(count)} }>count</button> */}
               
            </div>
        );
    
}

export default App
